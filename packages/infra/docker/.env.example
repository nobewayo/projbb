# Fastify server secrets
JWT_SECRET=change-me-please
POSTGRES_PASSWORD=bitby
CLIENT_ORIGIN=http://localhost

# Client build-time URLs
# Leave blank to default to the same origin that serves the client.
VITE_BITBY_HTTP_URL=
VITE_BITBY_WS_URL=

# Optional domain + HTTPS settings for the Caddy reverse proxy
CADDY_DOMAIN=
CADDY_EMAIL=
